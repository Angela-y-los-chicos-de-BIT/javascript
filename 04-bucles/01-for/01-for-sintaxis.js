/** Sintaxis del FOR
 * 
 * for( inicializador; condicion; actualizador (incremento/decremento)) ) {
 *      Sentencias que se van a iterar o repetir
 * }
 */

for( let i = 0; i < 10; i = i + 1 ) {
    console.log( i );
}